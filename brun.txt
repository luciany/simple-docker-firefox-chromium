XSOCK=/tmp/.X11-unix/X0

docker build -f Dockerfile.chrome --tag x . //build command for chrome
docker build --tag x .   //build command for chromium

docker run -it --privileged -v $XSOCK:$XSOCK  x firefox   // run firefox directly
docker run -it --privileged -v $XSOCK:$XSOCK  x chromium-browser  --no-sandbox    // run chromium directly
docker run -it --privileged -v $XSOCK:$XSOCK  x ./init    //run  google-chrome inside the container
        
              
